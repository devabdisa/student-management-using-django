# Render Environment Variables Setup

**Date:** May 9, 2026  
**Status:** Ready to Deploy

---

## 🔑 Complete Environment Variables for Render

Copy these **EXACT** values to your Render web service:

### Go to: Render Dashboard → Your Web Service → Environment Tab

Click **"Add Environment Variable"** for each of these:

---

### 1. DATABASE_URL
```
postgresql://dil_fere_user:OVttzMdxCYkKwbGFCWVKzKfWhIYjZ1Sb@dpg-d7v5o5lckfvc739tp9og-a/dil_fere_school
```

---

### 2. SECRET_KEY
```
2e2^5%u$n05p^gtbm+3xopl6-9w*%0wofafhig0o_204fze*b6
```

---

### 3. DEBUG
```
False
```

---

### 4. ALLOWED_HOSTS
```
.onrender.com
```

---

### 5. PYTHON_VERSION (Optional but Recommended)
```
3.11.0
```

---

## 📋 Quick Copy-Paste Format

If Render allows bulk import, use this format:

```env
DATABASE_URL=postgresql://dil_fere_user:OVttzMdxCYkKwbGFCWVKzKfWhIYjZ1Sb@dpg-d7v5o5lckfvc739tp9og-a/dil_fere_school
SECRET_KEY=2e2^5%u$n05p^gtbm+3xopl6-9w*%0wofafhig0o_204fze*b6
DEBUG=False
ALLOWED_HOSTS=.onrender.com
PYTHON_VERSION=3.11.0
```

---

## 🚀 Deployment Checklist

### Step 1: Add Environment Variables
- [ ] Go to Render Dashboard: https://dashboard.render.com/
- [ ] Select your web service
- [ ] Click "Environment" tab
- [ ] Add all 5 environment variables above
- [ ] Click "Save Changes"

### Step 2: Verify Build & Start Commands
- [ ] Go to "Settings" tab
- [ ] **Build Command:** `./build.sh`
- [ ] **Start Command:** `gunicorn student_management_system.wsgi:application`
- [ ] Click "Save Changes" if you made any updates

### Step 3: Deploy
- [ ] Go to "Manual Deploy" section
- [ ] Click "Deploy latest commit"
- [ ] Wait for build to complete (2-5 minutes)

### Step 4: Monitor Deployment
Watch the logs for these success indicators:

```
✅ Build successful 🎉
✅ Running migrations...
✅ Applying contenttypes.0001_initial... OK
✅ Applying auth.0001_initial... OK
✅ Applying main_app.0001_initial... OK
✅ Collecting static files...
✅ Starting gunicorn 22.0.0
✅ Listening at: http://0.0.0.0:10000
```

---

## 🧪 Test Your Deployment

After successful deployment:

1. **Visit your Render URL** (e.g., `https://your-app.onrender.com`)
2. **You should see the login page**
3. **Try logging in with admin credentials:**
   - Email: `admin@admin.com`
   - Password: `admin`

---

## ⚠️ Important Security Notes

### 🔒 Keep These Secret!
- **Never commit** these values to Git
- **Never share** your SECRET_KEY publicly
- **Never share** your DATABASE_URL publicly

### 🛡️ What's Protected
- `.env` file is in `.gitignore` ✅
- These values are only in Render's secure environment ✅
- Database password is auto-generated by Render ✅

---

## 🔄 If You Need to Update

### Update Environment Variables:
1. Go to Render Dashboard → Your Service → Environment
2. Click on the variable you want to change
3. Update the value
4. Click "Save Changes"
5. Render will automatically redeploy

### Rotate SECRET_KEY:
If you need a new SECRET_KEY, run locally:
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

---

## 🐛 Troubleshooting

### Build Fails
- Check that `build.sh` is executable
- Verify all dependencies are in `requirements.txt`
- Check build logs for specific errors

### App Won't Start
- Verify all 5 environment variables are set
- Check that DATABASE_URL is correct (no typos)
- Check that SECRET_KEY is set
- Look for errors in the logs

### Database Connection Errors
- Verify DATABASE_URL is the **Internal** URL (not External)
- Check that database is active (green status in Render)
- Ensure database and web service are in same region

### Static Files Not Loading
- Check that `build.sh` runs `collectstatic`
- Verify WhiteNoise is in `MIDDLEWARE` in settings.py
- Check that `STATIC_ROOT` is set correctly

---

## 📊 Expected Behavior

### First Deploy (Fresh Database)
```
Running migrations...
  Applying contenttypes.0001_initial... OK
  Applying auth.0001_initial... OK
  Applying main_app.0001_initial... OK
  Applying main_app.0002_alter_admin_id... OK
  Applying main_app.0003_alter_customuser_user_type... OK
  Applying main_app.0004_timeslot_timetable... OK
  ...
Collecting static files...
  Copied 1234 static files
```

### Subsequent Deploys
```
Running migrations...
  No migrations to apply.
Collecting static files...
  Copied 1234 static files
```

---

## 🎯 Next Steps After Deployment

1. **Create Superuser** (if needed)
   - You can't run `createsuperuser` directly on Render
   - Use the admin account from your local db.sqlite3
   - Or create via Django admin after first login

2. **Add Demo Data**
   - Login as admin
   - Add courses, subjects, staff, students
   - Or run `seed_demo_data.py` if you can access shell

3. **Test All Features**
   - Login as different user types
   - Test CRUD operations
   - Verify timetable works
   - Check attendance and results

4. **Monitor Performance**
   - Check response times
   - Monitor database usage
   - Watch for errors in logs

---

## 📝 Summary

**Environment Variables Set:**
- ✅ DATABASE_URL (PostgreSQL connection)
- ✅ SECRET_KEY (Django security)
- ✅ DEBUG (Production mode)
- ✅ ALLOWED_HOSTS (Render domain)
- ✅ PYTHON_VERSION (3.11.0)

**Ready to Deploy:** YES! 🚀

**Next Action:** Go to Render Dashboard and add these environment variables, then deploy!

---

**Need Help?** Check the logs in Render Dashboard → Your Service → Logs
